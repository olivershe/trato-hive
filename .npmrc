# Hoist @prisma/client and .prisma to root node_modules for consistent resolution
public-hoist-pattern[]=@prisma/client
public-hoist-pattern[]=.prisma

# Keep other dependencies isolated
shamefully-hoist=false
